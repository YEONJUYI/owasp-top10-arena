version: '3.8'

services:
  a01-broken-access:
    build: ./challenges/a01-broken-access
    ports:
      - "8001:5000"
    container_name: owasp-a01
    restart: unless-stopped
    
  # 나머지 챌린지는 만들면서 추가
  # a02-misconfiguration:
  #   build: ./challenges/a02-misconfiguration
  #   ports:
  #     - "8002:5000"
  #   container_name: owasp-a02
  #   restart: unless-stopped